# Bundling Notes

The mobile client uses Expo-managed Metro bundling.

## Mobile

- Start dev server: `npm start` (inside `frontend/mobile`)
- Android launch script: `npm run android`
- Web preview from Expo: `npm run web`

## Web Workspace

The `frontend/web` workspace is available for browser client evolution. If a custom bundler is introduced there, document it in this file.

