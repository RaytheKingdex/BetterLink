# Webpack Notes

Webpack is not currently used by the active mobile client (`frontend/mobile`) which relies on Expo/Metro.

If `frontend/web` adopts webpack in the future, document:

- Version and config files
- Build scripts
- Dev server command
- Asset handling rules
- Environment variable handling

