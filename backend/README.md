# Backend

ASP.NET Core backend service for BetterLink.

## Structure
- `Controllers/`: API endpoint controllers
- `Models/`: Domain and data transfer models
- `Services/`: Business logic and integrations
- `Program.cs`: Application entry point
- `Startup.cs`: Service registration and middleware pipeline

## Next Steps
- Add solution and project files (`.sln` and `.csproj`)
- Implement authentication and authorization
- Add data access and persistence layer
